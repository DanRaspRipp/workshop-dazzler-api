services:
  - type: web
    name: workshop-dazzler-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server_prod:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
